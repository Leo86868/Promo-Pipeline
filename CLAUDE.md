# PGC Pipeline

Roadmap discipline: `workflow/ROADMAP.md` is the position layer (milestones not PRs, single
writer, verify-before-write, done-migrate-out). Cross-repo (PGC ↔ AIGC asset_platform) handoffs →
`workflow/CROSS-REPO.md`. History trail → `workflow/daily-log.md`. Full protocol + scaffolding: the
`roadmap-discipline` skill.

Deep detail / design contracts (§翻转二) / full execution log → `docs/ROADMAP.md` (the heavy doc;
`workflow/ROADMAP.md` points to it). New-session 60-sec bootstrap also lives in its header.
